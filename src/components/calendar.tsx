import * as Calendar from 'expo-calendar';

import { View, Text } from "react-native";

export const CalendarView = () => {
  return (
    <View className="w-full h-80 p-4 bg-white rounded-md">
    </View>
  );
};
